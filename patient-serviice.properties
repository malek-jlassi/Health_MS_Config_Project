server.port=8584
spring.application.name=patient-serviice
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=patientDB

spring.cloud.compatibility-verifier.enabled=false

#spring.profiles.include=common
#spring.config.import=classpath:application-common.properties

# Eureka config
#eureka.client.service-url.defaultZone=http://localhost:8761/eureka
#eureka.instance.prefer-ip-address=true

# Enable dynamic refresh endpoint
management.endpoints.web.exposure.include=refresh

custom.message=Hello this is the patient service created by malek!
